<!-- SELECT YEAR(product.productDate) AS year, MONTH(product.productDate) AS month, DAY(product.productDate) AS day, SUM(product.quantity) AS totalProduct FROM product JOIN users ON product.userId = users.userId WHERE users.bakeryName = 'Ramsi' GROUP BY YEAR(product.productDate), MONTH(product.productDate), DAY(product.productDate) ORDER BY YEAR(product.productDate) DESC, MONTH(product.productDate) DESC, DAY(product.productDate) DESC; -->